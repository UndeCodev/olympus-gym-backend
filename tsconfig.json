{
  "compilerOptions": {
    "target": "ES6",                // Versión de JavaScript de salida
    "module": "commonjs",            // Sistema de módulos (para Node.js)
    "outDir": "./dist",              // Carpeta donde se guardará el build
    "rootDir": "./src",              // Carpeta fuente de tus archivos TS
    "strict": true,                  // Habilita todas las verificaciones de tipo
    "esModuleInterop": true,         // Compatibilidad con módulos ES
    "skipLibCheck": true             // Omite la verificación de tipos en librerías
  },
  "include": ["src/**/*"],           // Archivos a incluir
  "exclude": ["node_modules", "test"] // Archivos a ignorar
}